# Database
DATABASE_URL=postgresql://whoopaid:whoopaid_dev@localhost:5432/whoopaid

# App
NODE_ENV=development
PORT=3000

# JWT (generate with: openssl rand -base64 64)
JWT_SECRET=change_me_to_a_64_char_random_string

# Postgres container
POSTGRES_USER=whoopaid
POSTGRES_PASSWORD=whoopaid_dev
POSTGRES_DB=whoopaid
